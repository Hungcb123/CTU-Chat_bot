# Web API
fastapi==0.137.0
uvicorn==0.48.0
python-multipart==0.0.29
python-dotenv==1.2.2

# Database and storage
SQLAlchemy==2.0.50
asyncpg==0.31.0
psycopg2-binary==2.9.12
redis==8.0.0
qdrant-client==1.18.0
neo4j>=5.20.0
portalocker==3.2.0

# LLM, retrieval
langchain==1.3.2
langchain-classic==1.0.7
langchain-community==0.4.2
langchain-core==1.4.8
langchain-google-genai==4.2.4
# T1-T7: RAGAS imports langchain-openai; pin a release compatible with
# the project's langchain-core==1.4.8 instead of resolving to 1.6.0.
langchain-openai==1.1.10
langchain-qdrant==1.1.0
langchain-text-splitters==1.1.2
langgraph==1.2.2
# chromadb==1.5.9
bm25s>=0.3.10
pyvi>=0.1.1
# T1-T6: rag_engine still loads the local Vietnamese bi-encoder and checks
# torch for CPU/CUDA, so both packages are required by the current source.
torch>=2.2,<3
sentence-transformers>=3,<6

# T1-T7: benchmark runner and its regression tests.
ragas==0.2.15
pytest>=8,<10

# T1-T7: FastAPI 0.137.0 requires Pydantic >=2.9.0; pin a compatible 2.x release
# so the experiment environment can be installed reproducibly.
pydantic==2.12.5
typing_extensions==4.15.0

# Authentication and HTTP
bcrypt==5.0.0
PyJWT==2.13.0
httpx==0.28.1

# Utility scripts
numpy==1.26.4
scikit-learn==1.7.2
google-genai==2.8.0
PyYAML==6.0.3

uuid6
